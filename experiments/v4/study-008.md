# V4 study008: spatial robustness of paired competition

Freeze this protocol before running fresh environment seeds101000/101001.
Retain all60 frozen study005 descendant/founder pairs and their ancestry binding,
including55 equal-program pairs. No selection by study006 or007 performance.

Cross each pair with two environments, drive250/500, three role-site layouts
and two role placements:1440 runs. On the8x8 periodic lattice, normal layouts
are adjacent(34,35), intermediate(34,36), separated(34,38); reversed placement
swaps the two sites. These compare specific horizontal layouts, not every
orientation or translation, and cannot isolate distance from all site effects.

Use unchanged v4-competition-1 with200 steps; programs ordered descendant then
actual founder. Both founders material0/energy64; initial_raw1 everywhere,
capacity64,drive_amount8,leak1,bond_cost1,exchangeTrue,threshold16,
construction_cost4,copy_cost1,mutation0. Match per-site random streams across
layouts and placements. Record all states, physical ledgers and passive roles.

Primary score remains sum of role0-minus-role1 living counts over ticks1..200,
divided by12800, including founders and post-extinction zeros. Average placements
first (720 scores), then three selected pairs and two environments within each
training source, assay drive and layout (120 source means), then five training
seeds per training drive/mutation, assay drive and layout (24 group means).
Predefine adjacent-minus-separated and intermediate-minus-separated differences
within source before averaging seeds (16 group contrasts). Report all signs,
exact fractions and source values, without significance or graduation criteria.
Fresh separated-layout results are an independent-environment replication;
comparison to study007 is descriptive, not a matched-environment contrast.

Require660 equal-program layout/environment/drive controls with physical byte
identity, reversed role counts and exact zero placement average. Independently
reconstruct dynamics and roles for all1440 runs. Check exact grid, declared
founder programs/sites, physical pairing, random states, selection/protocol and
clean source binding. Preserve all negative/neutral outcomes and missing cases.

Use12864 site records per run and a6GiB cohort soft limit checked between runs;
one-run overshoot is possible. Stop on audit failure or limit, retain partial
data and declare incomplete. Archive execution first, then full verification.
No runtime-law changes during the cohort. Results concern these fixed layouts
and two-founder competition, not arbitrary ecology, organism-level reproduction
or open-ended evolution. The256-program hereditary space remains finite.
