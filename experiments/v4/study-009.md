# V4 study009: descriptive boundary dependence on retained hereditary worlds

Freeze this selection and summary plan before observing cohort structure outputs.
Use every original study005 world: seeds96000..96004 crossed with drive250/500
and mutation0/100, twenty retained500-tick16x16 trajectories. Re-audit dynamics
and verify source hashes against archived study005 execution records. This is
retrospective observation of those worlds, not twenty new simulations or a new
independent training cohort. No selection by observed organization or fitness.

Use the phase-aligned structure observer and independent union-find audit. Each
world must yield2502 partition checks and2497 continuity comparisons, including
the initial final-state geometry. Keep raw member identities and boundaries
passive. Save full observation and audit records outside source directories;
require source bytes unchanged. Record source, observer, auditor and protocol
hashes. Stop and mark incomplete if any source/audit fails; do not omit it.

Primary descriptive measure: per-tick fraction of occupied interaction-phase
units whose complete component member set differs between material-contact and
realized-bond definitions. Average over ticks101..500 within a world, then average
the five seed means separately for each drive/mutation condition. Empty states
have null fractions: report their count and the nonempty denominator, and leave
a world mean null if all ticks are empty. Report group coverage explicitly;
do not silently substitute zero or hide an unavailable seed.

Secondary observations in windows1..100 and101..500:

- All three interaction boundary definitions: mean component count, mean largest
  component size divided by occupied units, and mean singleton-unit fraction.
- Final geometric contact/material definitions: the same measures.
- Interaction contact-minus-material membership disagreement.
- Same-phase overlap splits, merges and components without surviving-member
  overlap, as total counts over available transitions. Interaction transitions
  begin at tick2; final transitions begin at tick1 against initialization. Window
  assignment uses the destination tick. No-overlap is not no ancestry; splits
  are not counted as reproduction.

Publish per-source summaries and the four condition summaries, preserving
seed-level heterogeneity. No significance threshold, monotonicity requirement,
graduation criterion or optimal-boundary choice. Bond dependence on energy is
part of the engineered rules; disagreement alone does not prove organization.
This establishes the sensitivity of a descriptive instrument before designing
a structural heredity or replication hypothesis. Do not infer causal effects
from these summaries or treat alternative definitions as independent replicates.

Bound total new observation output at2GiB, checked between complete worlds with
possible one-world overshoot. Include failures and partial coverage in status.
Use existing sources only; no additional scientific random seeds are consumed.
